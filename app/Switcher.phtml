<?php

namespace Appliance;

function Switcher()
{
    return (<<< HTML
    <Router>
        <Route method="GET" rule='/' exact="true" redirect="Home" >
            <Authorization />
        </Route>
        <Route method="GET" rule='/landing' exact="true" redirect="Landing" />
    </Router>
    HTML);
}
